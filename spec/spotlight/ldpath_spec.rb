require 'spec_helper'

describe Spotlight::Ldpath do
  it 'has a version number' do
    expect(Spotlight::Ldpath::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
